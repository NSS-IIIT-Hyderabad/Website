# Direct dependencies only - let pip resolve the transitive tree itself.
# (This file previously carried ~30 hand-pinned transitive packages, plus
# unused ones like redis/python-ldap/phonenumbers/pytz, which is why the
# install pulled down far more than the app actually needs.)

fastapi==0.115.12
uvicorn==0.34.0

# strawberry-graphql[cli] provides the `strawberry export-schema` command
# used by init.sh/dev-entrypoint.sh; [fastapi] provides GraphQLRouter.
strawberry-graphql[fastapi]
strawberry-graphql[cli]

pymongo==4.13.2
pydantic>=2.11.2,<3.0.0
email_validator>=2.2.0
python-multipart>=0.0.20

PyJWT>=2.9.0
python-cas

psutil
prometheus-client

# Test dependencies
pytest
httpx
